const rows = document.querySelector('.row')
const creation = document.querySelector('.circle')
rows.addEventListener('click', () => {
    // console.log('printed')
    if (str.style.display === "none") {
        return x.style.display = "block";
        document.querySelector('.circle.active')
      }
})